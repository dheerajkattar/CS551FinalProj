CS551 Final Project: Cloud workload benchmarking with a current pivot to LLM-first VM deployment.

## Current Focus: LLM on EC2 + GCE

Primary implementation lives in `Applications/LLM_APP`.

Deployment artifacts:

- `Applications/LLM_APP/DEPLOYMENT_CONTRACT.md`
- `deploy/llm_vm/bootstrap.sh`
- `deploy/llm_vm/deploy_parallel.sh`
- `deploy/llm_vm/verify.sh`
- `deploy/llm_vm/EC2_GCE_PARITY.md`

## Implementation Status

- LLM runtime hardened for VM deployment:
  - runtime-safe Gemini init
  - health/readiness semantics
  - request timeout and basic rate limiting controls
  - request latency logging
- Simultaneous deployment workflow for one EC2 VM and one GCE VM is scripted in `deploy/llm_vm`.

## Benchmarking (Next Step)

Benchmark harness remains in:

- `benchmarks/README.md`
- `benchmarks/runner.py`

For the upcoming matrix, use `benchmarks/LLM_BENCHMARK_INPUTS.md` to provide benchmark parameters.
